// a file that contains re usable constants

// strings
export const baseUrl = '/api/v1';

// error messages
export const noToken = 'token not provided';
export const notFound = 'not found';
export const notAuthenticated = 'not authenticated';
export const notAllowed = 'not allowed';
export const invalidToken = 'token content is invalid';
export const invalidCredentials = 'invalid credentials';
export const passwordErrorMessage =
  'should have a minimum of five characters, at least one letter and one number';
export const emailExistsError = 'user with the email already exists';
// success strings
export const itemExists = 'item with given name, price and vendor already exists';
